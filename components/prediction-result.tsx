import { PredictResponse, PredictRequest } from "@/lib/api";
import Image from "next/image";
import { TrendingUp, Award, Download, ArrowRight, Activity, Thermometer, Calendar } from "lucide-react";

interface PredictionResultProps {
  data: PredictResponse;
  inputData?: PredictRequest; // Used to show metadata section
  onNewPrediction: () => void;
}

export function PredictionResult({ data, inputData, onNewPrediction }: PredictionResultProps) {
  // Mock Confidence Score
  const confidence = "94.2%";
  
  const handleDownload = () => {
    if (!data) return;
    
    const content = `
AGRIFORECAST PREDICTION REPORT
------------------------------
Date: ${new Date().toLocaleString()}
Crop: ${inputData?.crop || 'N/A'}
Region: ${inputData?.region || 'N/A'}

PREDICTION RESULTS
------------------
Predicted Yield: ${data.yield_prediction.toFixed(2)} Quintals/Acre
Predicted Price: ₹${data.price_prediction.toLocaleString()} per Quintal
Price Trend: ${data.price_trend.toUpperCase()}
Confidence Level: ${confidence}

ENVIRONMENTAL PARAMETERS
------------------------
Avg Temperature: ${inputData?.temperature_celsius || 'N/A'}°C
Avg Rainfall: ${inputData?.rainfall_mm || 'N/A'} mm
Soil Type: ${inputData?.soil_type || 'N/A'}
Growth Window: ${inputData?.days_to_harvest || 'N/A'} Days

AI AGRONOMIST ADVISORY
----------------------
${data.advisory}

Generated by AgriForecast Satellite Relay
    `;
    
    const blob = new Blob([content], { type: 'text/plain' });
    const url = URL.createObjectURL(blob);
    const link = document.createElement('a');
    link.href = url;
    link.download = `AgriForecast_Report_${inputData?.crop || 'Crop'}_${new Date().getTime()}.txt`;
    document.body.appendChild(link);
    link.click();
    document.body.removeChild(link);
    URL.revokeObjectURL(url);
  };
  
  return (
    <div className="space-y-6 w-full max-w-5xl animate-in fade-in slide-in-from-bottom-4 duration-700 ease-out">
      <div className="flex items-center justify-between mb-8">
        <div>
          <span className="bg-[var(--color-gold)]/10 text-[var(--color-gold)] border border-[var(--color-gold)]/30 px-3 py-1.5 rounded-sm text-[10px] font-bold tracking-widest uppercase shadow-sm inline-block mb-3">
            FORECAST GENERATED: {new Date().toLocaleDateString('en-US', { month: 'short', day: 'numeric', year: 'numeric' })}
          </span>
          <h2 className="text-3xl font-bold text-[#E8F0E4] tracking-tight text-balance">
            Prediction Results: <span className="text-[var(--color-gold)]">{inputData ? `${inputData.crop} Harvest` : 'Harvest'}</span> {new Date().getFullYear()} {inputData?.region ? `(${inputData.region} Region)` : ''}
          </h2>
          <p className="text-sm text-[#CBD5C9] mt-3 max-w-2xl leading-relaxed italic opacity-90">
            Analysis based on provided regional parameters, historical yield data, and current Mandi rates for {inputData?.region || 'your region'}.
          </p>
        </div>
      </div>

      <div className="grid grid-cols-1 md:grid-cols-[1fr_0.7fr] gap-6">
        
        {/* Yield Card with background */}
        <div className="relative rounded-xl overflow-hidden shadow-[0_8px_32px_rgba(0,0,0,0.4)] h-64 border border-[var(--color-gold)]/30 glass-dark group">
          <Image 
            src="/images/smart_crop.png" 
            alt="AI smart crop analysis" fill className="object-cover opacity-[0.15] mix-blend-plus-lighter group-hover:scale-105 transition-transform duration-700" unoptimized={false} 
          />
          <div className="absolute inset-0 bg-gradient-to-r from-[var(--color-primary-900)] via-[var(--color-primary-900)]/80 to-transparent" />
          <div className="absolute inset-0 border-l-4 border-[var(--color-gold)]" />
          
          <div className="relative z-10 p-8 flex flex-col h-full justify-between">
            <div>
              <p className="text-[10px] font-bold text-[var(--color-gold)] uppercase tracking-widest mb-3 flex items-center gap-2">
                <Activity className="w-3.5 h-3.5" /> PREDICTED YIELD
              </p>
              <div className="flex items-baseline gap-2">
                <span className="text-6xl font-black text-[#E8F0E4] tracking-tighter drop-shadow-md">{data.yield_prediction.toFixed(1)}</span>
                <span className="text-sm font-bold text-[#CBD5C9] uppercase tracking-widest opacity-80">Quintals / Acre</span>
              </div>
            </div>
            <div className="flex items-center justify-between text-xs text-[#E8F0E4]">
              <span className="flex items-center gap-1.5 font-bold bg-[var(--color-primary-800)]/60 px-3 py-1.5 rounded border border-[var(--color-gold)]/20 shadow-sm">
                <TrendingUp className="w-3.5 h-3.5 text-[var(--color-gold)]"/> 
                <span className="text-[var(--color-gold)]">+8.2%</span> vs Region Avg.
              </span>
              <span className="font-medium opacity-70 italic tracking-wide">Confidence Level: {confidence}</span>
            </div>
          </div>
        </div>

        {/* Price Card with background */}
        <div className="relative rounded-xl overflow-hidden shadow-[0_8px_32px_rgba(0,0,0,0.4)] h-64 border border-[var(--color-gold)]/30 bg-[var(--color-primary-800)]/80 group">
          <Image 
            src="/images/dashboard_overview.png" 
            alt="Market aesthetic" fill className="object-cover opacity-10 mix-blend-overlay group-hover:scale-105 transition-transform duration-700" unoptimized={false} 
          />
          
          <div className="relative z-10 p-8 flex flex-col h-full bg-gradient-to-b from-transparent to-[var(--color-primary-900)]/50">
            <p className="text-[10px] font-bold text-[var(--color-gold)] uppercase tracking-widest mb-3 flex items-center gap-2">
              <span className="w-1.5 h-1.5 rounded-full bg-[var(--color-gold)] animate-pulse" /> MARKET OUTLOOK
            </p>
            <div className="mb-2 mt-auto">
              <span className="text-5xl font-black text-[#E8F0E4] tracking-tighter drop-shadow-[0_2px_4px_rgba(0,0,0,0.5)]">
                ₹{data.price_prediction.toLocaleString('en-IN', {minimumFractionDigits: 0, maximumFractionDigits: 0})}
              </span>
            </div>
            <p className="text-[10px] font-bold text-[#CBD5C9] uppercase tracking-widest mb-auto opacity-70">Selling Price (₹/Quintal)</p>
            
            <div className="mt-6 bg-[var(--color-primary-900)]/80 text-[var(--color-gold)] text-[10px] uppercase tracking-widest font-bold px-4 py-2.5 rounded-sm border border-[var(--color-gold)]/30 backdrop-blur-sm flex items-center gap-2 max-w-max shadow-sm">
              {data.price_trend === 'up' ? <TrendingUp className="w-4 h-4"/> : <Activity className="w-4 h-4"/>}
              Mandi Trend: <span className="capitalize">{data.price_trend === 'up' ? 'Rising' : data.price_trend}</span>
            </div>
          </div>
        </div>
      </div>

      {/* Recommendation Card */}
      <div className="border-l-4 border-[var(--color-gold)] glass-dark shadow-[0_10px_30px_rgba(0,0,0,0.3)] p-8 rounded-r-xl relative overflow-hidden my-8">
        <div className="absolute right-0 top-0 w-64 h-64 bg-[var(--color-gold)]/5 rounded-full blur-3xl" />
        <div className="flex justify-between items-start mb-6 relative z-10">
          <div className="flex items-center gap-5">
            <div className="w-14 h-14 rounded-xl bg-[var(--color-gold)]/10 border border-[var(--color-gold)]/30 flex items-center justify-center text-[var(--color-gold)] shadow-[0_0_15px_rgba(212,160,23,0.15)] flex-shrink-0">
              <Award className="w-7 h-7" />
            </div>
            <div>
              <div className="flex items-center gap-4">
                <h3 className="text-2xl font-bold text-[#E8F0E4] tracking-tight">AI Agronomist Advisory</h3>
                <span className="bg-red-500/10 border border-red-500/30 text-red-400 px-2.5 py-1 text-[9px] font-bold tracking-widest uppercase rounded-sm shadow-sm">PRIORITY ACTION</span>
              </div>
            </div>
          </div>
        </div>
        
        <div className="relative z-10 text-[#E8F0E4] leading-relaxed text-[15px] font-medium max-w-4xl ml-20 bg-[var(--color-primary-800)]/60 p-5 rounded-lg border border-[var(--color-gold)]/20 shadow-inner">
          <span className="text-[var(--color-gold)] font-bold mb-2 block uppercase tracking-wide text-xs">Recommended Action</span>
          <span className="opacity-90">{data.advisory}</span>
        </div>

        <div className="flex gap-4 mt-8 ml-20 relative z-10">
          <button onClick={onNewPrediction} className="btn-gold uppercase tracking-widest text-[11px] px-8 py-3.5 rounded-sm shadow-lg hover:scale-[1.02] transition-transform">
            Approve Strategy & New Prediction
          </button>
          <button 
            onClick={handleDownload}
            className="flex items-center gap-2 bg-transparent border border-[#CBD5C9]/30 text-[#E8F0E4] px-8 py-3.5 rounded-sm text-[11px] font-bold uppercase tracking-widest hover:bg-[#CBD5C9]/10 transition-colors shadow-sm"
          >
            <Download className="w-4 h-4" /> Download Report
          </button>
        </div>
      </div>

      {/* Input Parameters / Metadata */}
      <div>
        <p className="text-[10px] font-bold text-[var(--color-gold)] uppercase tracking-widest mb-5 flex items-center gap-3">
          <span className="w-4 h-[1px] bg-[var(--color-gold)]/50" />
          INPUT PARAMETERS & METADATA
          <span className="flex-1 h-[1px] bg-gradient-to-r from-[var(--color-gold)]/50 to-transparent" />
        </p>
        
        <div className="grid grid-cols-1 md:grid-cols-[1fr_0.8fr] gap-6">
          <div className="space-y-4">
            {/* Small metric cards */}
            <div className="grid grid-cols-2 sm:grid-cols-4 gap-4">
              <div className="bg-[var(--color-primary-800)]/40 p-4 rounded-lg border border-[var(--color-gold)]/10 text-center hover:bg-[var(--color-primary-800)]/60 transition-colors">
                <p className="text-[9px] font-bold text-[var(--color-gold)] tracking-widest uppercase mb-1.5 drop-shadow-sm">ANNUAL RAINFALL</p>
                <p className="text-xl font-black text-[#E8F0E4]">{inputData?.rainfall_mm || '450'} <span className="text-[10px] opacity-70">MM</span></p>
              </div>
              <div className="bg-[var(--color-primary-800)]/40 p-4 rounded-lg border border-[var(--color-gold)]/10 text-center hover:bg-[var(--color-primary-800)]/60 transition-colors">
                <p className="text-[9px] font-bold text-[var(--color-gold)] tracking-widest uppercase mb-1.5 drop-shadow-sm">REGION / AREA</p>
                <p className="text-xl font-black text-[#E8F0E4]">{inputData?.region || 'NORTH'}</p>
              </div>
              <div className="bg-[var(--color-primary-800)]/40 p-4 rounded-lg border border-[var(--color-gold)]/10 text-center hover:bg-[var(--color-primary-800)]/60 transition-colors">
                <p className="text-[9px] font-bold text-[var(--color-gold)] tracking-widest uppercase mb-1.5 drop-shadow-sm">AVG TEMP</p>
                <p className="text-xl font-black text-[#E8F0E4]">{inputData?.temperature_celsius || '19.2'}°C</p>
              </div>
              <div className="bg-[var(--color-primary-800)]/40 p-4 rounded-lg border border-[var(--color-gold)]/10 text-center hover:bg-[var(--color-primary-800)]/60 transition-colors">
                <p className="text-[9px] font-bold text-[var(--color-gold)] tracking-widest uppercase mb-1.5 drop-shadow-sm">PRE-HARVEST</p>
                <p className="text-xl font-black text-[#E8F0E4]">{inputData?.days_to_harvest || '112'} Days</p>
              </div>
            </div>
            
            {/* Dark NDVI map block */}
            <div className="w-full h-36 bg-[var(--color-primary-900)] rounded-lg relative overflow-hidden flex items-center justify-center border border-[var(--color-gold)]/20 shadow-inner group">
              <Image src="/images/dashboard_overview.png" alt="Map" fill className="object-cover opacity-[0.35] mix-blend-luminosity grayscale group-hover:scale-105 transition-transform duration-[2s]" unoptimized={false} />
              <div className="relative z-10 bg-[var(--color-primary-900)]/80 backdrop-blur-md border border-[var(--color-gold)]/40 px-5 py-2.5 rounded-sm flex items-center gap-3 shadow-lg">
                <div className="w-2 h-2 rounded-full bg-[var(--color-gold)] shadow-[0_0_10px_#D4A017] animate-pulse" />
                <span className="text-[var(--color-gold)] text-[10px] font-bold tracking-widest uppercase">LIVE NDVI ANALYSIS {(inputData?.region || "NORTH INDIA").toUpperCase()}</span>
              </div>
            </div>
          </div>

          <div className="relative rounded-lg overflow-hidden min-h-[160px] shadow-lg border border-[var(--color-gold)]/20 group">
            <Image 
              src="/images/aerial_farm.png" 
              alt="Crop close up" fill className="object-cover group-hover:scale-105 transition-transform duration-[2s]" unoptimized={false} 
            />
            <div className="absolute inset-0 bg-gradient-to-t from-black via-black/40 to-transparent" />
            <div className="absolute bottom-5 left-5 right-5">
              <span className="bg-[var(--color-primary-900)]/80 border border-[var(--color-gold)]/30 backdrop-blur text-[var(--color-gold)] text-[9px] font-bold tracking-widest px-2.5 py-1 rounded-sm uppercase mb-2 inline-block shadow-lg">HOSHIARPUR PLOT {inputData?.crop ? inputData.crop.substring(0,3).toUpperCase() : 'B'}-2</span>
              <p className="text-[#E8F0E4] font-bold text-base tracking-wide drop-shadow-md">Visual confirmation of grain ripening.</p>
              <p className="text-[#CBD5C9] text-[10px] mt-1 font-medium tracking-wide">Captured via local drone survey, {new Date().toLocaleDateString('en-US', { month: 'short', day: 'numeric'})}.</p>
            </div>
          </div>
        </div>
      </div>
    </div>
  );
}
